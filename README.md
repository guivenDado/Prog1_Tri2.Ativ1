# Prog1_Tri2.Ativ1

- no windows
 - cria a pasta
 - coloca cmd na barra de endereço
 -então executar isso ae
```bash
git clone https://<<git-token>>@github.com/<<rafinhagames/prog1.tri2.ativ1>> projeto

```bash
code -user-data-dir=vscode-data projeto
```
# ...

```bash
git push
```

# ...
```bash
git add -all
git commit -m "bla bla bla que foi feito em aula"
git push